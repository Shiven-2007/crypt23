import { List } from "postcss/lib/list";

export {};

declare global {
  interface leveldatatype {
    section: string;
    level: string;
    branch: string;
  }
}
export declare module "*.mp4" {
  const src: string;
  export default src;
}
